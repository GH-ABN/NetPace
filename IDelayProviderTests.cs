//
// TestWeaver Generated Tests
//
// Auto-generated by DeeperCore TestWeaver
// File: IDelayProviderTests.cs
// Generated: 2025-12-04T16:34:00.096242
//
// These tests cover edge cases and boundary conditions discovered
// through TestWeaver's 5-phase analysis methodology.
//

using System;

public class IDelayProviderTests
{
[Fact]
public async Task DelayAsync_WithPositiveMilliseconds_ShouldDelayForSpecifiedTime()
{
    // Arrange
    var delayProvider = new DelayProvider();
    var delay = 100; // 100 milliseconds
    var startTime = DateTime.UtcNow;

    // Act
    await delayProvider.DelayAsync(delay);
    var elapsedTime = DateTime.UtcNow - startTime;

    // Assert
    Assert.True(elapsedTime.TotalMilliseconds >= delay, 
        $"Delay should be at least {delay}ms, but was {elapsedTime.TotalMilliseconds}ms");

[Fact]
public async Task DelayAsync_CanceledDuringDelay_ThrowsTaskCanceledException()
{
    // Arrange
    var delayProvider = new DelayProvider();
    var cts = new CancellationTokenSource();
    var delayTask = delayProvider.DelayAsync(1000, cts.Token);

    // Act
    cts.Cancel();

    // Assert
    await Assert.ThrowsAsync<TaskCanceledException>(() => delayTask);

[Fact]
public async Task DelayAsync_WithDefaultCancellationToken_ShouldDelayForSpecifiedTime()
{
    // Arrange
    var delayProvider = new DelayProvider();
    int delayMilliseconds = 100;
    var stopwatch = new System.Diagnostics.Stopwatch();

    // Act
    stopwatch.Start();
    await delayProvider.DelayAsync(delayMilliseconds);
    stopwatch.Stop();

    // Assert
    Assert.True(stopwatch.ElapsedMilliseconds >= delayMilliseconds, 
        $"Delay should be at least {delayMilliseconds}ms, but was {stopwatch.ElapsedMilliseconds}ms");

[Fact]
public async Task DelayAsync_WithZeroMilliseconds_CompletesImmediately()
{
    // Arrange
    var delayProvider = new DelayProvider();
    var stopwatch = new System.Diagnostics.Stopwatch();

    // Act
    stopwatch.Start();
    await delayProvider.DelayAsync(0);
    stopwatch.Stop();

    // Assert
    Assert.True(stopwatch.ElapsedMilliseconds < 10, $"DelayAsync with 0 milliseconds took {stopwatch.ElapsedMilliseconds}ms, which is longer than expected.");

[Fact]
public async Task DelayAsync_WithNonCancelledToken_CompletesAfterSpecifiedDelay()
{
    // Arrange
    var delayProvider = new DelayProvider();
    var delayMilliseconds = 100;
    var cts = new CancellationTokenSource();
    var stopwatch = new System.Diagnostics.Stopwatch();

    // Act
    stopwatch.Start();
    await delayProvider.DelayAsync(delayMilliseconds, cts.Token);
    stopwatch.Stop();

    // Assert
    Assert.True(stopwatch.ElapsedMilliseconds >= delayMilliseconds, 
        $"Delay should be at least {delayMilliseconds}ms, but was {stopwatch.ElapsedMilliseconds}ms");

[Fact]
public async Task DelayAsync_WithVerySmallMillisecondValue_ShouldDelayBriefly()
{
    // Arrange
    var delayProvider = new DelayProvider();
    var stopwatch = new System.Diagnostics.Stopwatch();

    // Act
    stopwatch.Start();
    await delayProvider.DelayAsync(1);
    stopwatch.Stop();

    // Assert
    Assert.True(stopwatch.ElapsedMilliseconds >= 1, $"The delay should be at least 1ms, but it was {stopwatch.ElapsedMilliseconds}ms");
    Assert.True(stopwatch.ElapsedMilliseconds < 50, $"The delay should be less than 50ms for a 1ms request, but it was {stopwatch.ElapsedMilliseconds}ms");

[Fact]
public async Task DelayAsync_WithVeryLargeMilliseconds_ShouldNotThrowException()
{
    // Arrange
    var delayProvider = new DelayProvider();
    var veryLargeMilliseconds = int.MaxValue;
    var cts = new CancellationTokenSource(TimeSpan.FromMilliseconds(100)); // Short timeout for test

    // Act & Assert
    try
    {
        var delayTask = delayProvider.DelayAsync(veryLargeMilliseconds, cts.Token);
        await Assert.ThrowsAsync<TaskCanceledException>(() => delayTask);

    catch (Exception ex)
    {
        Assert.True(false, $"DelayAsync should not throw an exception for very large millisecond values. Exception: {ex}");

    Assert.True(cts.IsCancellationRequested, "The cancellation token should have been requested due to timeout");

[Fact]
public async Task DelayAsync_WithVerySmallDelay_ShouldCompleteQuickly()
{
    // Arrange
    IDelayProvider delayProvider = new DelayProvider();
    int verySmallDelay = 1; // 1 millisecond
    var stopwatch = new System.Diagnostics.Stopwatch();

    // Act
    stopwatch.Start();
    await delayProvider.DelayAsync(verySmallDelay);
    stopwatch.Stop();

    // Assert
    Assert.True(stopwatch.ElapsedMilliseconds >= verySmallDelay, 
        $"The delay should be at least {verySmallDelay} ms, but it was {stopwatch.ElapsedMilliseconds} ms");
    Assert.True(stopwatch.ElapsedMilliseconds < 100, 
        $"The delay should complete quickly, but it took {stopwatch.ElapsedMilliseconds} ms");
}
